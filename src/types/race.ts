export type RaceFormData = {
  raceName: string;
  date: string;
  raceDistance: string;
  predictedTime: string;
  actualTime: string;
  isPB: boolean;
  shoes: string;
};

export type Race = {
  raceName: string;
  date: string;
  raceDistance: string;
  predictedTime: string;
  actualTime: string;
  isPB: boolean;
  shoes: string;
};